const Profile = () => {
    return (
      <div>
        <h2>Profile</h2>
        {/* Form to update personal details like name, email, etc. */}
      </div>
    );
  };
  
  export default Profile;
  